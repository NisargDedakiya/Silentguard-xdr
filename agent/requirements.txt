psutil>=5.9
requests>=2.31
# Test dependency — kept here so `pip install -r requirements.txt && pytest -q`
# passes with no manual extras.
pytest>=8.0
# scapy is optional — used for active ARP verification when available
# scapy>=2.5
# Optional USB monitoring backends (a dependency-free /sys/block poller is
# used on Linux when pyudev is not installed):
# pyudev>=0.24   (Linux)
# wmi; pywin32   (Windows)
