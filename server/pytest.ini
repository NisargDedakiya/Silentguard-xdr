[pytest]
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
markers =
    unit: fast, isolated logic tests (no app/HTTP)
    api: endpoint tests via the TestClient
    integration: multi-component flows (agent↔server, ingestion→detection)
    security: security-control tests (auth, RBAC, SSRF, tenancy)
