fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
python-multipart>=0.0.9
# Test dependencies — kept here so `pip install -r requirements.txt && pytest -q`
# passes with no manual extras.
pytest>=8.0
pytest-asyncio>=0.23
httpx2>=2.0  # required by starlette's TestClient
# Optional production extras:
# psycopg2-binary  (PostgreSQL)
# redis, celery    (async blocklist push / isolation jobs)
